# Windows Zone.Identifier files (WSL download metadata)
*Zone.Identifier

# Environment / secrets
.env
.env.local
.boukensha/

# Python virtualenv (shared at repo root across all python/ steps)
.venv/
__pycache__/
*.pyc
*.gem

# Local native Windows TUI dependency builds
.tui_windows_build/
